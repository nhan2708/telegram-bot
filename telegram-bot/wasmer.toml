[package]
name = "telegram-bot"
version = "1.0.0"
description = "Telegram AI bot"

[dependencies]
nodejs = "*"

[[command]]
name = "start"
module = "nodejs"
runner = "wasi"
args = ["bash", "-lc", "npm install -g openclaw && openclaw gateway"]
